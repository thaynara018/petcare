name: app_clinica_veterinaria
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  device_preview: ^1.3.1
  device_preview_plus: ^2.5.5
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  
  flutter_localizations: # Adicione esta linha
    sdk: flutter
  intl: 0.20.2 # Adicione esta linha para formatar dd/mm/aaaa

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo_branca.png
    - assets/logo_verde.png
    - assets/pedro.png
    - assets/thaynara.png

